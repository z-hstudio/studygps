# No environment variables are required for the local priority engine.
# Add placeholders here when the n8n integration needs configuration.
# Keep real credentials in local environment files or n8n credentials.
